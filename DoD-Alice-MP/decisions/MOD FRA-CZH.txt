political_decisions = {
	bridge_over_salzach = {
	picture = salzach_bridge
		potential = {
			tag = BOH
			DUA = { test = is_monarchy_government }
			NOT = { has_global_flag = salzachrefused }
			NOT = { has_global_flag = salzach }
			NOT = { has_country_flag = salzachongoing }
			is_greater_power = yes
			DUA = { is_greater_power = yes }
			NOT = { exists = ITA }
			NOT = { exists = ARI }
			NOT = { exists = GER }
			NOT = { exists = SGF }
		}
		allow = {
			test = is_monarchy_government
			NOT = { truce_with = DUA }
			NOT = { war_with = DUA }
			ideological_thought = 1
			war = no
			is_vassal = no
			ARN = { is_greater_power = no }
		}
		effect = {
			set_country_flag = salzachongoing
			relation = { who = BRG value = -100 }
			relation = { who = PBG value = -100 }
			relation = { who = SCA value = -100 }
			relation = { who = ARN value = -100 }
			DUA = { country_event = 598000 }
		}
		ai_will_do = {
			factor = 1
		}
	}

	core_provence = {
		picture = valois_italy
		potential = {
			tag = DUA
			NOT = { has_country_flag = provence_cored }
		}

		allow = {
			test = is_colonial_power
			owns = 470
			NOT = { exists = PRV }
			NOT = { exists = AVN }
		}

		effect = {
			prestige = 5
			set_country_flag = provence_cored
			FRA_467 = { add_core = DUA }
		}

		ai_will_do = {
			factor = 1
		}
	}

	meddle_in_italy = {
		picture = DUA_italy
		potential = {
			tag = DUA
			NOT = { has_country_flag = meddle_in_italy }
			is_greater_power = yes
			NOT = { exists = ITA }
			NOT = { exists = AUV }
		}

		allow = {
			has_global_flag = JEAN_ENDING
			OR = {
				has_country_flag = ITALY_OPEN
				NOT = { exists = BRG }
			}
			war = no
			PRV = {
				exists = no
				all_core = { owned_by = THIS }
			}
		}

		effect = {
			prestige = 5
			relation = { who = ARN value = -200 }
			set_country_flag = meddle_in_italy
		}

		ai_will_do = {
			factor = 1
		}
	}
}